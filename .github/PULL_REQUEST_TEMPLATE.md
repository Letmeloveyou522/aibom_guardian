<!--
읽어 주셔서 감사합니다. 아래는 리뷰를 빠르게 하기 위한 것이고,
해당 없는 항목은 지우셔도 됩니다.
-->

## 무엇을 바꿨나요

<!-- 한두 줄로. 관련 이슈가 있으면 "Closes #12" 처럼 적어 주세요. -->

## 왜 이렇게 했나요

<!--
이 PR에서 가장 중요한 칸입니다.

무엇을 했는지는 diff가 말해 줍니다. 판단이 개입한 곳 - 왜 다른 방식이
아니라 이 방식인지, 무엇을 포기했는지 - 을 적어 주세요. 나중에 이 코드를
만질 사람에게는 맥락이 없습니다.
-->

## 어떻게 확인했나요

<!--
- [ ] `pytest` 전부 통과
- [ ] 동작이 바뀌었다면 테스트를 추가했습니다
- [ ] 실제로 실행해 봤습니다 (명령과 결과를 붙여 주세요)
-->

```
```

## 판정에 영향을 주나요

<!--
점수·verdict·라이선스 분류가 바뀐다면 여기에 적어 주세요. 같은 입력에
어제와 다른 답이 나오는 변경은 리포트 비교를 깨뜨립니다.

바뀌지 않으면 "없음" 한 단어면 됩니다.
-->

없음

## 체크리스트

- [ ] 미검증 항목을 통과(`ALLOW`) 처리하지 않습니다 — `None`을 `[]`로
      바꾸는 코드가 없습니다
- [ ] 점수 판정은 여전히 `score_engine.py`에서만 합니다
- [ ] 테스트가 네트워크를 쓰지 않습니다
- [ ] 의존성을 추가했다면 `pyproject.toml` · `requirements.txt` ·
      `DEPENDENCIES.md` 세 곳을 모두 고쳤습니다
- [ ] 새 주석은 영어이고, 무엇이 아니라 왜를 설명합니다
