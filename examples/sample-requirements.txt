# Example scan input for AIBOM-Guardian.
#
#     python3 scanner.py examples/sample-requirements.txt
#
# WARNING: do not `pip install -r` this file. The versions below are pinned
# to known-vulnerable releases on purpose, so a demo scan has something to
# report. The project's own dependencies are in ../requirements.txt.

requests==2.28.0    # several CVEs incl. Proxy-Authorization leak
numpy==1.24.0       # clean: exercises the ALLOW path
pyyaml==5.3.1       # CVE-2020-14343, critical - exercises the hard block
# CI aibom-scan job scans this file; the pyyaml pin above is expected to fail the gate.
